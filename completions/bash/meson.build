bash_completion_dir = join_paths(get_option('datadir'), 'bash-completion', 'completions')

install_data('ws-copy', install_dir: bash_completion_dir)
install_data('ws-paste', install_dir: bash_completion_dir)
